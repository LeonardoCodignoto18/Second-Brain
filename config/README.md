# Runtime configuration

Versioned, non-secret configuration schemas and defaults belong here when required.
Secrets never belong in this directory. Runtime configuration must be validated at
the application boundary, and environment variables are reserved for development or
deployment mechanics rather than product state.

