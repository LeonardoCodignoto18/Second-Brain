{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Minimal capability set for the main local presentation window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
