/**
 * Non-product shell used only to prove the presentation process can start.
 * Product screens are deliberately absent from Foundation-01.
 */
export function FoundationShell() {
  return <main aria-label="Second Brain OS" />;
}
