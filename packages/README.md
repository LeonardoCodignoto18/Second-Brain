# Frontend packages

This directory is reserved for generated IPC bindings or frontend packages with more
than one real consumer. Canonical contracts originate in Rust. TypeScript must not
redefine domain meaning. No package is created until generation or reuse is present.

